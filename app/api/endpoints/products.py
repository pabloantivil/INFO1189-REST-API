from fastapi import APIRouter, HTTPException, Depends
from typing import List
from app.core.config import settings

# Crear el router
router = APIRouter()

# Aquí se definirán los endpoints relacionados con productos
